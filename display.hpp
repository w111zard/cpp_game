#ifndef DISPLAY_HPP
#define DISPLAY_HPP

#include <string>

#include "level.hpp"

void display_level(std::vector<std::vector<game_object_t>> data);
void display_set_size(size_t h, size_t w);

#endif // DISPLAY_HPP
